<?xml version="1.0" encoding="UTF-8"?>
<Calculator:ModalView xmlns:Calculator="clr-namespace:Calculator"
                      xmlns="http://xamarin.com/schemas/2014/forms"
                      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                      xmlns:d="http://xamarin.com/schemas/2014/forms/design"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      mc:Ignorable="d"
                      x:Class="Calculator.TutorialDialog"
                      Padding="20, 20, 20, 10"
                      BackgroundColor="White">
    <StackLayout Orientation="Vertical">
        <Label x:Name="DescriptionLabel"
               VerticalOptions="CenterAndExpand"
               FontSize="Large"
               HorizontalTextAlignment="Center" />

        <ContentView x:Name="Showing"
                     VerticalOptions="CenterAndExpand">
            <StackLayout Spacing="50">
                <Label Text="Welcome to Crunch!"
                       HorizontalTextAlignment="Center"
                       FontSize="33" />
                <Label Text="A calculator for the digital age"
                       HorizontalTextAlignment="Center"
                       FontSize="20" />
            </StackLayout>
            <!--<Label Padding="20, 20"
                   BackgroundColor="Red"
                   HorizontalTextAlignment="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Welcome to Crunch!"
                              FontSize="33" />
                        <Span Text="&#10;&#10;A calculator for the digital age"
                              FontSize="20" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>-->
        </ContentView>

        <StackLayout Orientation="Horizontal">
            <Button x:Name="Back"
                    Text="Back"
                    BackgroundColor="Transparent" />

            <Button x:Name="Next"
                    HorizontalOptions="EndAndExpand"
                    BackgroundColor="Transparent" />
        </StackLayout>
    </StackLayout>
</Calculator:ModalView>